{
  "name": "tow-not",
  "private": true,
  "version": "1.0.0",
  "description": "AlertNav — live traffic and dispatch incident aggregator for London, ON",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"npm run dev -w @tow-not/server\" \"npm run dev -w @tow-not/client\"",
    "dev:server": "npm run dev -w @tow-not/server",
    "dev:client": "npm run dev -w @tow-not/client",
    "build": "npm run build -w @tow-not/server",
    "build:server": "npm run build -w @tow-not/server",
    "start": "npm run start -w @tow-not/server",
    "typecheck": "npm run typecheck -w @tow-not/server && npm run typecheck -w @tow-not/client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  },
  "allowScripts": {
    "esbuild@0.28.2": true,
    "esbuild@0.25.12": true
  }
}
